[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    requires_torch: test depends on torch + transformers being importable
    requires_model: test depends on a trained model artifact under ML/models/
