# Dev / CI tooling.
# Runtime deps live in requirements.txt — this file extends that for tests + linting.

-r requirements.txt

# Test runner
pytest>=8.0
pytest-cov>=5.0

# Lint + format — pinned so local and CI format identically (ruff's formatter
# changes between minors; an unpinned CI once reformatted a file the pinned dev
# ruff had left alone and went red).
ruff==0.16.2

# Secret scanner used by pre-commit
detect-secrets>=1.5

# Pre-commit framework
pre-commit>=3.7
